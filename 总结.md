# 顺序实施交付总结

日期：2026-07-16
分支：`codex/sequential-roadmap`  
独立工作区：`D:\try one\VS Code\DIS\.vercel\worktrees\codex-sequential`

## 1. 本次完成范围

按照已确认的顺序完成：**P0 喜运达绑定闭环 → P1 预测式补货 → P7 全球库存作战室 → 首页决策看板**。未改动 Product → ProductVariant → Inventory 核心模型，也未让页面或客户端组件直接访问 Supabase。

### P0：喜运达未绑定记录闭环

- 在 `/dashboard/shipments/import/golucky` 展示尚未绑定内部 Shipment 的外部物流记录。
- 按外部记录的国家和仓库查询同仓同国 Shipment 候选。
- 通过 Server Action 调用既有绑定 RPC，继续由数据库校验登录状态、账号状态、仓库权限、国家/仓库一致性和并发重复绑定。
- UI 明确提示绑定不可逆；绑定成功后立即从未绑定列表移除并刷新页面。
- 保留既有换仓保护：绑定后的 Shipment 不允许更换仓库。

主要文件：

- `src/features/in-transit/components/golucky-unbound-records.tsx`
- `src/features/in-transit/actions.ts`
- `src/features/in-transit/repository.ts`
- `src/app/dashboard/shipments/import/golucky/page.tsx`

### P1：预测式补货引擎

- 新增仓库参数：`buffer_ratio`、`target_cover_multiplier`、`updated_at`。
- 新增计划发货软取消字段 `shipment.cancelled_at`，不扩展现有状态枚举。
- 新增共享数据库函数 `forecast_stockout(...)`，统一处理 ETA 事件、同日到货聚合、晚到不抵扣、无日销、无 lead time 和有效在途数量。
- 新增 `get_in_transit_detail` 与 `get_replenishment_suggestions` 两个读取 RPC。
- 新增 `/dashboard/replenishment`：筛选、分页、紧急度、补货数量、预计断货日、最晚下单日、ETA 明细。
- Admin 可维护仓库补货参数、创建 booking 计划发货和软取消计划；Operator 只读已授权仓库。
- 内部计划单号由服务端生成，唯一约束冲突仅针对 `shipment_no_unique` 最多重试 3 次。

Migration：

- `00041_replenishment_warehouse_params.sql`
- `00042_replenishment_cancellation.sql`
- `00043_forecast_stockout.sql`
- `00044_replenishment_rpcs.sql`

### P7：全球库存作战室

- 新增唯一路由 `/dashboard/products/overview`。
- 新增唯一列表 RPC `get_product_overview`，以 inventory 为驱动表，在数据库内完成仓库权限、聚合、搜索、国家筛选、断货风险、稳定排序、分页和信封级计数。
- 新增唯一详情 RPC `get_war_room_variant_detail`，详情只返回当前用户可见仓库。
- P7 详情逐仓复用 P1 的 `get_replenishment_suggestions`，没有复制补货公式。
- 页面包含决策队列、库存表、国内库存“待接入”占位、KPI、分国 burn-down、仓库级补货行动。
- `stockout_urgency` 与 P1 的 `replenishment_urgency` 分离；`partial_data` 保持独立布尔值。

Migration：

- `00045_product_overview_rpc.sql`
- `00046_war_room_variant_detail_rpc.sql`

### 首页决策看板

- 新增 `get_warehouse_health_overview` RPC，以 warehouse + variant 库存位置为统计粒度。
- 当前用户已归档 SKU 在分类和汇总前排除；Operator 仅统计已授权仓库。
- 首页改为六路独立加载：库存健康、ETA 已知计划及在途、低库存、关注产品、未来 7 日到港、同步状态。
- KPI 使用 P1 在途口径，不再使用旧的全量在途聚合。
- 未来 7 日到港展示 Top4，但总数来自完整在途明细去重，不使用 Top4 长度冒充总数。
- 增加仓库健康 Dialog、低库存 Top5、关注 Top4，以及同步、补货、全球库存快捷入口。

Migration：

- `00047_dashboard_warehouse_health_overview.sql`

## 2. 架构与安全检查

- 读取链保持 Server Component → Repository → Supabase → PostgreSQL RLS。
- 客户端懒加载保持 Client → Server Action → Repository → RPC/RLS。
- 新增 UI 中没有 `createClient`、`supabase.from()`、`.rpc()` 或 `service_role`。
- 新增 UI 与业务代码没有使用 `any`。
- P1/P7/首页 RPC 均绑定 `auth.uid()` 与服务端传入的 `p_user_id`，并校验活动账号和角色。
- 新 RPC 使用 `SECURITY INVOKER`、`SET search_path = ''`、撤销 PUBLIC/anon 执行权限，仅授权 authenticated。
- 未修改已经执行的 Migration；全部数据库变更使用 00041–00047 新 Migration。

## 3. 验证结果

- 目标功能测试：P0/P1/P7/首页共 **200/200** 通过。
- 受影响旧契约回归：**235/235** 通过。
- 第一轮全量测试：**3879/3879**，87 个文件，0 失败。
- 第二轮全量测试：**3879/3879**，87 个文件，0 失败。
- ESLint：**0 errors / 31 warnings**；warning 均为既有或已知项，本批次无新增 error。
- `next build`：编译、TypeScript、静态页面生成全部通过；新路由已进入构建产物。
- `git diff --check`：通过，无尾随空格或冲突标记。
- 浏览器冒烟：登录页正常；`/dashboard`、`/dashboard/replenishment`、`/dashboard/products/overview` 未登录时均正确带 redirect 返回登录页；最终控制台 **0 error/warn**。
- React 自审：Hooks 生命周期、交互唯一性、可访问名称、减少动态效果、空/错/加载状态均已检查。
- Supabase 部署：`00041 → 00042 → 00043 → 00044 → 00045 → 00046 → 00047` 已严格按序应用到 `DIS Project`，远端记录 7 条 migration。
- Supabase Staging：新建 `DIS Staging`（project ref `hyarhvsjhkjpallbyifn`，Singapore），从空数据库严格重放 `00001 → 00047`，47 条 migration 历史连续完整。
- Staging 安全复验：18/18 个 public 基础表启用 RLS；六个 P1/P7/首页 RPC 均为 `SECURITY INVOKER`、空 `search_path`、anon 无执行权、authenticated 有执行权；安全与性能顾问均为 0 error。
- 环境一致性：表/RLS 状态、策略和触发器与 Production 一致；Staging 按当前 migration 链额外拥有 00010/00011 的归档字段、索引/外键和 `claim_sync_run_system(...)`，已记录为 Production 历史漂移，未在本任务中直接修补生产库。
- 数据库对象：4 个新列、2 个约束、2 个索引、1 个触发器和 6 个 RPC 均存在；新函数全部为 `SECURITY INVOKER`、空 `search_path`、anon 无执行权、authenticated 有执行权。
- 数据兼容：6 个仓库的新参数均满足约束；2 条既有 Shipment 的 `cancelled_at` 保持 NULL；未发现旧数据被取消或破坏。
- 真实身份只读冒烟：Admin/Operator 的补货、在途、P7 列表/详情和首页健康度 RPC 均返回正确 JSON 契约；Operator 只看到 1 个已分配仓库，四类跨仓库泄露断言全部通过。
- 迁移后顾问：安全与性能顾问均为 0 error，且没有本批新增函数或索引相关告警；既有顾问项不在本次 migration 范围内。

构建中仍有两类既有非阻塞 warning：独立 worktree 的多 lockfile root 推断，以及既有同步 Cron 链导致的 NFT 动态文件追踪提示。两者未造成编译或类型失败。

## 4. 剩余事项与已知限制

- 数据库 migration、只读 RPC/RLS 冒烟和独立 Staging 全量重放已经完成；Vercel Preview 尚未切换到 Staging，Production 环境变量保持不变。应先完成 Preview 接线并验证真实页面，再决定合并/生产发布。
- Admin 仓库参数、计划发货与取消属于写入流程，本次数据库验收保持只读，仍需在 Preview 页面完成真实写入与回滚走查。
- Production migration 历史在本次执行前为空：00001–00040 早期通过 SQL Editor 执行但未登记；当前仅登记 00041–00047。Staging 已证明完整 migration 链可重放，同时确认 Production 缺失部分 00010/00011 对象。未来启用 Supabase CLI `db push` 或补齐生产对象前需先做 migration history baseline/repair 与影响评审。
- P8 国内库存接入和 P7-C 国内补给判断仍按原路线保留，不在本批次内。
- 百世 API 的外部 partnerId 权限仍是独立外部阻塞，不影响本批次代码。

## 5. 部署与验收顺序

1. ✅ 已在目标 Supabase 严格执行：`00041 → 00042 → 00043 → 00044 → 00045 → 00046 → 00047`。
2. ✅ 已创建 `DIS Staging` 并从空库严格执行 `00001 → 00047`，完成 RLS、函数权限、顾问和 migration 历史验收。
3. 下一步只把 `codex/sequential-roadmap` 的 Vercel Preview Supabase 变量切换到 `DIS Staging`，不修改 Production target，也不先提升 Production。
4. 使用 Staging Admin 验证仓库参数、计划发货、取消、P7 详情和首页真实数据。
5. 使用 Staging Operator 验证只读限制和 warehouse assignment 隔离。
6. 核对取消/吸收/已入仓记录不再进入有效在途；ETA 缺失只进入可见在途、不进入有效补给。
7. 验证通过后再合并分支并提升 Production；失败时按 F→E→D→C→B→A 的依赖方向评估回滚，禁止修改已执行 Migration 文件。

## 6. 2026-07-16 Preview 接线补充

- Vercel Preview 的 `NEXT_PUBLIC_SUPABASE_URL` 与 `NEXT_PUBLIC_SUPABASE_ANON_KEY` 已仅在 Preview 环境指向 `DIS Staging`，Production 的同名变量及其他生产变量均未修改。
- 本次分支文档提交会通过 Git 集成触发新的 Preview 部署，使公开 Staging 配置生效。
- Staging `SUPABASE_SERVICE_ROLE_KEY` 尚未配置到 Preview，测试 Admin/Operator 尚未创建；因此页面登录、服务端写入与角色/RLS 全流程验收仍未完成。
- 下一步需登录 Supabase Dashboard 取得 Staging Service Role，仅写入 Vercel Preview，然后创建测试身份并执行 Admin/Operator 页面与写入冒烟。完成前不合并、不提升 Production。
- 分支 Preview 部署 `dpl_6qrYQDEE1aJg8mfVAPqnSx4Dw33S` 已达到 READY；`/auth/login` 返回 HTTP 200，原“Supabase URL/Key 缺失”500 已消失，部署级 error/fatal/warning 运行日志为空。
